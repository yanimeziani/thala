# ========================================
# Thala Backend Environment Configuration
# ========================================
# This file is ready to use for local development!
# Just add your external API keys below and run: docker-compose up
# NEVER commit the .env file to version control!

# ========================================
# Database Configuration (Auto-configured for Docker)
# ========================================
POSTGRES_USER=thala
POSTGRES_PASSWORD=YbgdwCSWmX7qtM6cXqAi
POSTGRES_DB=thala
POSTGRES_PORT=5432

# Full database URL (auto-configured for Docker Compose)
# For local Docker: db is the service name
# For production: update the host to your database URL
DATABASE_URL=postgresql+asyncpg://thala:YbgdwCSWmX7qtM6cXqAi@db:5432/thala

# ========================================
# Authentication & Security
# ========================================
# JWT secret key - Pre-generated secure key (change in production!)
JWT_SECRET=99d224e68e4abbd160bd52d52ba06590551668fe534128c605a66c3295a3ae40

# JWT token expiration times (in minutes)
ACCESS_TOKEN_EXPIRATION_MINUTES=60
REFRESH_TOKEN_EXPIRATION_MINUTES=20160

# JWT algorithm
JWT_ALGORITHM=HS256

# ⚠️ REQUIRED: Google OAuth Client ID (get from Google Cloud Console)
# https://console.cloud.google.com/apis/credentials
GOOGLE_OAUTH_CLIENT_ID=your_google_oauth_client_id.apps.googleusercontent.com

# ========================================
# S3 Storage Configuration
# ========================================
# Using MinIO for local development (auto-configured)
AWS_REGION=us-east-1
AWS_S3_BUCKET=thala-media
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=J9LMxCtv5wVUkTpcdcrS

# S3 endpoint (configured for local MinIO in Docker)
S3_ENDPOINT_URL=http://minio:9000

# For production AWS S3: Remove S3_ENDPOINT_URL and set:
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key

# ========================================
# MeiliSearch Configuration (Auto-configured)
# ========================================
MEILISEARCH_HOST=http://meilisearch:7700
MEILISEARCH_API_KEY=122ff50f0b715b6713101746a90af6f25e4b074cc8d45bf0c73ff6c9d282b365
MEILISEARCH_INDEX_PREFIX=thala

# MeiliSearch environment settings
MEILI_ENV=production
MEILI_NO_ANALYTICS=true

# ========================================
# MinIO Configuration (Auto-configured)
# ========================================
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=J9LMxCtv5wVUkTpcdcrS
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001

# ========================================
# CORS Configuration
# ========================================
# Comma-separated list of allowed origins
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,https://yourdomain.com

# ========================================
# Rate Limiting
# ========================================
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS_PER_MINUTE=60
RATE_LIMIT_BURST=10

# ========================================
# Application Settings
# ========================================
APP_NAME=Thala Backend
API_V1_PREFIX=/api/v1
LOG_LEVEL=INFO
BACKEND_PORT=8000

# ========================================
# Nginx (Production)
# ========================================
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443
